function check(form) {
    if ( form.email.value == "admin" && form.password.value == "admin") ? alert ("Login successfully"); window.location = "www.facebook.com" : alert("Attempt Login failed");
}